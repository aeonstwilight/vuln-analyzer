# Copy this to .env and fill in values
# Currently no secrets required for local development.
# This file is provided for future configuration (e.g. auth, database URL).

# API host (used if deploying backend separately from frontend)
# API_HOST=http://localhost:8000

# Log level
# LOG_LEVEL=INFO
